{
  "name": "dev-dock-server",
  "version": "0.1.0",
  "type": "module",
  "description": "Cross-platform (Linux/macOS/Windows) headless dev-dock: serves the PWA + WebSocket bridge and drives Claude Code, terminals, files and project history. Use it from the browser or the VS Code extension.",
  "bin": {
    "dev-dock-server": "./index.mjs"
  },
  "scripts": {
    "start": "node index.mjs",
    "new-token": "node scripts/new-token.mjs",
    "postinstall": "node scripts/fix-node-pty.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "node-pty": "^1.0.0",
    "qrcode-terminal": "^0.12.0",
    "web-push": "^3.6.7",
    "ws": "^8.16.0"
  }
}
